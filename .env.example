# Copy to .env and fill in (never commit .env)
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=

# Must match a Redirect URI in your Spotify Dashboard exactly
SPOTIFY_REDIRECT_URI=http://localhost:7654/callback
# SPOTIFY_PORT=7654

# Optional — default ~/.aux-mcp
# AUX_MCP_TOKEN_DIR=
